# name: Build and Publish Docusaurus Site to GH pages

# on:
#   push:
#     branches:
#       - main

# permissions:
#   contents: read
#   pages: write
#   id-token: write

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "18.13.0"

#       - name: Install dependencies
#         run: npm install

#       - name: Build the site
#         run: npm run build

#       - name: Upload artifact
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: build

#   deploy:
#     runs-on: ubuntu-latest
#     needs: build
#     permissions:
#       pages: write # to deploy to GitHub Pages
#       id-token: write # to authenticate on behalf of GitHub Actions

#     steps:
#       - name: Deploy to GitHub Pages
#         uses: actions/deploy-pages@v4
